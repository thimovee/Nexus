import React from 'react'

const page = () => {
    // This page will contain a list of all the attribute tables (Color, Size, Brand, Category). The user will see the main groups of attributes and will be able to click on them to see the values (red, blue, green etc..) and create, remove, update and delete them.
    return (
        <div>page</div>
    )
}

export default page